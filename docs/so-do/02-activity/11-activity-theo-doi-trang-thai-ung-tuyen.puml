@startuml
skinparam defaultFontName "Segoe UI"
skinparam defaultFontSize 13
skinparam shadowing false
skinparam linetype ortho
skinparam activity {
  BackgroundColor White
  BorderColor #222222
  DiamondBackgroundColor White
  DiamondBorderColor #222222
  BarColor #111111
}
start
:Ứng viên đăng nhập;
:Mở trang hồ sơ ứng tuyển;
fork
  :Hệ thống tải toàn bộ HồSơỨngTuyển rồi lọc theo ứng viên ở client;
fork again
  :Hệ thống tải TinTuyểnDụng và NhàTuyểnDụng liên quan;
fork again
  :Hệ thống tải LịchPhỏngVấn riêng rồi ghép theo hồ sơ ứng tuyển;
end fork
if (Tải dữ liệu thành công?) then (Đúng)
  :Hiển thị danh sách hồ sơ ứng tuyển;
  fork
    :Nhập từ khóa tìm theo vị trí hoặc công ty;
  fork again
    :Chọn bộ lọc trạng thái;
  end fork
  :Hệ thống lọc động danh sách phía client;
  if (Chọn một hồ sơ?) then (Có)
    :Mở chi tiết HồSơỨngTuyển;
    if (Trạng thái cho phép rút?) then (Có)
      if (Nhấn "Rút hồ sơ"?) then (Có)
        :Nhập lý do nếu có;
        :Hệ thống cập nhật trạng thái sang đã rút;
        :Hiển thị thông báo rút hồ sơ thành công;
      else (Không)
        :Tiếp tục xem chi tiết;
      endif
    else (Không)
      :Chỉ hiển thị tiến trình và lịch liên quan;
    endif
  else (Không)
    :Tiếp tục xem danh sách;
  endif
else (Sai)
  :Hiển thị thông báo không tải được hồ sơ ứng tuyển;
endif
stop
@enduml
